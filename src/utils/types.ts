export interface CityInfo {
    value: string;
    label: string;
    latitude: number;
    longitude: number;
  }

 export interface DistanceInfo {
    startCity: string;
    finalCity: string;
    distance: number;
  }
  